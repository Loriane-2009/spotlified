// Un Service Worker est un script JavaScript qui fonctionne en arrière-plan, indépendamment de l’application. 
// Il permet notamment de :
// - Mettre en cache des fichiers pour une utilisation hors ligne.
// - Gérer les requêtes réseau et améliorer les performances.
// - Envoyer des notifications push.

self.addEventListener('install', (event) => { })
self.addEventListener('activate', (event) => { })
self.addEventListener('fetch', (event) => { })